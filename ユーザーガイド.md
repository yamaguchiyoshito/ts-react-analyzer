# ts-react-analyzer ユーザーガイド

## 結論

`ts-react-analyzer` は、React / TypeScript プロジェクトの構造負債を短時間で可視化するための CLI です。  
使い方は 2 系統です。  
構造解析は `analyze` / `graph` / `diff`、出荷審査は `quality collect` / `quality gate` / `quality diff` を使います。

このツールは次を解析します。

- `.ts`
- `.tsx`
- `.js`
- `.jsx`

## 先に押さえるべき仕様

- 相対パスの基準は、このツールのリポジトリではなく `projectDir` です
- 既定の出力先は `<projectDir>/analysis-reports` です
- 既定のキャッシュ先は `<projectDir>/.ts-analyzer-cache` です
- 既定のログ出力先は `<projectDir>/analysis.log` です
- `diff` で `--baseline` を省略すると、既定で `<outputDir>/<prefix>_report.json` を読みます
- `graph` は `--format` の指定に関係なく、常に `*_graph.json` と `*_graph.dot` を出力します
- `diff` は成功時 `0`、一般エラー時 `1`、impact 閾値超過時 `2` を返します
- `quality gate` は親指標の自動 `FAIL`、または baseline 比較での親指標回帰時に `2` を返します
- 品質レポートの証跡 `filePath` は `projectDir` 基準の相対パスで出力されます

## 前提条件

- Node.js 22 以上を推奨
- `npm` または `pnpm`

## セットアップ

このリポジトリをそのまま使う場合の手順です。

```bash
npm install
npm run build
```

以後の実行例は、すべてこのリポジトリ直下で打つ前提です。

## 最短手順

解析対象が `./sample-app` の場合、最短手順はこれです。

```bash
node dist/src/cli.js analyze ./sample-app
node dist/src/cli.js graph ./sample-app
node dist/src/cli.js diff ./sample-app --baseline ./sample-app/analysis-reports/analysis_report.json
```

まず `analyze` で基準レポートを生成し、その JSON を baseline として `diff` に渡すのが正攻法です。

出荷審査まで回す最短手順はこれです。

```bash
node dist/src/cli.js quality collect ./sample-app
node dist/src/cli.js quality gate ./sample-app --baseline ./sample-app/analysis-reports/analysis_quality_report.json
```

`quality collect` は品質レポート生成、`quality gate` は出荷判定です。`--baseline` は前回リリースなど既存の `*_quality_report.json` を指す前提です。baseline 比較を入れると「今回悪化したか」まで機械判定できます。

## コマンド一覧

### 1. analyze

単発解析です。複雑度、依存、hot spot、CSV、HTML を出します。

```bash
node dist/src/cli.js analyze <projectDir>
```

実用例:

```bash
node dist/src/cli.js analyze ./sample-app \
  --output ./reports \
  --prefix main \
  --format json,markdown,html,csv
```

この例の出力先は `./sample-app/reports` です。  
`./reports` はツールのカレントディレクトリ基準ではなく、`projectDir` 基準で解決されます。

### 2. graph

依存グラフ専用の出力です。

```bash
node dist/src/cli.js graph <projectDir>
```

実用例:

```bash
node dist/src/cli.js graph ./sample-app --output ./reports --prefix deps
```

出力ファイル:

- `deps_graph.json`
- `deps_graph.dot`

`graph` は `--format` を見ません。JSON と DOT を必ず出します。

### 3. diff

baseline と現在の差分を比較します。

```bash
node dist/src/cli.js diff <projectDir> --baseline <report.json>
```

実用例:

```bash
node dist/src/cli.js diff ./sample-app \
  --output ./reports \
  --prefix main \
  --baseline ./sample-app/reports/main_report.json
```

CI ゲート例:

```bash
node dist/src/cli.js diff ./sample-app \
  --output ./reports \
  --prefix main \
  --baseline ./sample-app/reports/main_report.json \
  --impact-threshold 60 \
  --fail-on-impact
```

この場合、優先度付き impacted file の `score >= 60` が 1 件でもあれば終了コード `2` を返します。

### 4. quality collect / report / gate / diff

出荷審査用の品質レポート系です。

```bash
node dist/src/cli.js quality collect <projectDir>
node dist/src/cli.js quality gate <projectDir>
node dist/src/cli.js quality diff <projectDir> --baseline <quality_report.json>
```

実用例:

```bash
node dist/src/cli.js quality collect ./sample-app \
  --output ./reports \
  --prefix release \
  --format json,markdown,html,csv

node dist/src/cli.js quality gate ./sample-app \
  --output ./reports \
  --prefix release \
  --baseline ./sample-app/reports/release_quality_report.json
```

この系統の仕様は次です。

- `collect` と `report` は同じ品質レポート生成です
- `gate` は親指標の自動 `FAIL` が 1 件でもあれば終了コード `2`
- `gate --baseline` は親指標の自動回帰も検知して終了コード `2`
- `quality diff` の件数集計も親指標だけを数え、派生指標は診断情報として保持
- `quality.manual.json` を置くと手動証跡を自動マージ
- レポート中の証跡 `filePath` は `projectDir` 基準の相対パスで出力

現在の自動取込対象:

- `tsconfig.json` ベースの TypeScript 型エラー
- `coverage/lcov.info` などの LCOV
- `junit.xml` などの JUnit XML
- `package.json` / `vitest.config.*` / `vitest.workspace.*` からの Vitest 検出
- `reports/axe*.json` の axe
- `reports/lighthouse*.json` の Lighthouse
- `playwright-report/results.json` の Playwright
- `reports/storybook*.json` の Storybook
- `openapi-diff.json` の OpenAPI diff
- `npm-audit.json` の `npm audit --json`
- `trivy-results.json` の Trivy

## 推奨運用

### 初回導入

```bash
node dist/src/cli.js analyze ./target-app --output ./analysis --prefix baseline
node dist/src/cli.js graph ./target-app --output ./analysis --prefix baseline
```

これで基準点を作ります。以後は `baseline_report.json` を保管してください。

### 日次または PR ごとの確認

```bash
node dist/src/cli.js diff ./target-app \
  --output ./analysis \
  --prefix current \
  --baseline ./target-app/analysis/baseline_report.json
```

見るべき順番は固定です。

1. `current_diff.md`
2. `current_diff.html`
3. `current_report.md`
4. `current_files.csv`
5. `current_dependencies.csv`

理由は明確です。  
差分を先に見ないと、単なる絶対値の大きさと今回の変更影響が混ざります。

### 出荷審査

```bash
node dist/src/cli.js quality collect ./target-app --output ./analysis --prefix release
node dist/src/cli.js quality gate ./target-app \
  --output ./analysis \
  --prefix release \
  --baseline ./target-app/analysis/release_quality_report.json
```

見る順番は次です。

1. `release_quality_report.md`
2. `release_quality_diff.md`
3. `release_quality_summary.csv`

品質レポートは `FAIL` 件数だけでなく、`PARTIALカテゴリ` と `MANUAL` を見ないと誤読します。自動で緑でも、未収集証跡が多ければ出荷判定としては弱いです。

## オプション

共通オプション:

- `--output <dir>`: 出力ディレクトリ
- `--format <formats>`: `csv,markdown,json,html,all`
- `--config <path>`: 設定ファイルパス
- `--prefix <name>`: 出力ファイル接頭辞
- `--verbose`: 詳細ログを有効化
- `--max-file-size <bytes>`: 指定サイズ超のファイルを除外
- `--complexity-threshold <n>`: 複雑度警告閾値
- `--exclude-patterns <patterns>`: カンマ区切り正規表現
- `--cache-dir <dir>`: キャッシュディレクトリ
- `--log-file <path>`: ログファイルパス
- `--manual-input <path>`: 手動品質証跡 JSON パス
- `--quality-gate-blocking-metrics <ids>`: baseline 回帰で gate を落とす指標 ID
- `--quality-gate-monitoring-metrics <ids>`: baseline 回帰を監視専用にする指標 ID
- `--help`: ヘルプ表示

`diff` 専用:

- `--baseline <path>`: baseline の `*_report.json`
- `--impact-threshold <n>`: impact score 閾値
- `--fail-on-impact`: 閾値超過時に終了コード `2`

`quality` 専用:

- `--manual-input <path>`: 未指定時は `<projectDir>/quality.manual.json` を自動読込
- `--baseline <path>`: `quality gate` / `quality diff` の比較元 `*_quality_report.json`
- `--quality-gate-blocking-metrics <ids>`: baseline 回帰で gate を落とす指標 ID
- `--quality-gate-monitoring-metrics <ids>`: baseline 回帰を監視専用にする指標 ID

## 出力ファイル

### analyze の出力

- `<prefix>_report.json`: 完全な機械処理向けレポート
- `<prefix>_report.md`: 人間向けサマリー
- `<prefix>_report.html`: 依存グラフ付き HTML
- `<prefix>_files.csv`: ファイル単位の一覧
- `<prefix>_dependencies.csv`: import / export / dynamic import 一覧
- `<prefix>_components.csv`: React コンポーネント一覧
- `<prefix>_hooks.csv`: Hook 利用集計

### graph の出力

- `<prefix>_graph.json`: ノード、エッジ、graph metrics
- `<prefix>_graph.dot`: Graphviz / DOT

### diff の出力

- `<prefix>_diff.json`: 差分の完全データ
- `<prefix>_diff.md`: 差分サマリー
- `<prefix>_diff.html`: 影響サブツリーの可視化

補足:

- `diff` 実行時は差分ファイルだけでなく、現在状態の `*_report.*` も同時に更新されます
- `diff` の差分出力は `--format` では制御されません。JSON / Markdown / HTML が常に生成されます

### quality collect / quality report / quality gate の出力

- `<prefix>_quality_report.json`: 出荷審査向けの品質レポート。証跡 `filePath` は相対パス
- `<prefix>_quality_report.md`: 品質報告書向け Markdown。`PARTIALカテゴリ`、親指標 / 派生指標を含む
- `<prefix>_quality_report.html`: ブラウザ閲覧向け品質レポート
- `<prefix>_quality_summary.csv`: 品質指標一覧

### quality diff の出力

- `<prefix>_quality_diff.json`: 品質差分の完全データ。件数集計は親指標のみ
- `<prefix>_quality_diff.md`: リリース比較向け差分
- `<prefix>_quality_diff.html`: ブラウザ閲覧向け品質差分

## どのレポートをどう読むか

### まず見るファイル

- `*_report.md`: 全体の意思決定サマリー
- `*_diff.md`: 今回変更の影響
- `*_report.html`: 依存グラフを視覚的に確認

### 深掘りに使うファイル

- `*_files.csv`: 負債候補の横比較
- `*_components.csv`: JSX と Hook の偏り確認
- `*_dependencies.csv`: fan-out と循環依存の根拠確認
- `*_hooks.csv`: Hook 利用の偏在確認
- `*_quality_report.md`: 出荷観点の阻害要因確認
- `*_quality_diff.md`: 前回リリース比の悪化確認

### 優先順位の付け方

優先的に見るべきシグナルは次です。

1. `diff` の impacted files で score が高い
2. `report.md` の hot spot Top 5 に入っている
3. `files.csv` で `Matrix Cluster` が `L-H` に寄っている
4. graph warnings に循環依存、hub、fan-out が出ている
5. 型安全性で `any`、assertion、non-null assertion、`ts-ignore` が多い

この順番で見れば、単なるコード量ではなく、変更コストが高い箇所から潰せます。

### 品質レポートの見方

見る順番は固定です。

1. `OVERALL`
2. `FAIL`
3. `PARTIALカテゴリ`
4. `MANUAL`
5. 各カテゴリの親指標
6. 派生指標

`PARTIAL` は「自動判定はきれいだが、手動証跡不足で出荷判定を閉じられない」です。  
派生指標は gate 件数には入れません。たとえば `Route / Feature / Form / UI` のテスト対応率は診断用で、総合の `matching_test_file_presence` が親指標です。

## 設定ファイル

`<projectDir>/analyzer.config.json` を置くと、毎回 CLI オプションを書かずに済みます。

```json
{
  "outputDir": "./analysis-reports",
  "filePrefix": "analysis",
  "outputFormats": ["json", "markdown", "html"],
  "complexityThreshold": 10,
  "impactScoreThreshold": 60,
  "failOnImpactThreshold": false,
  "maxFileSizeBytes": 10485760,
  "cacheDir": "./.ts-analyzer-cache",
  "logFile": "./analysis.log",
  "manualInputPath": "./quality.manual.json",
  "qualityGateBlockingMetricIds": ["secret_indicators", "dependency_vulnerabilities"],
  "qualityGateMonitoringMetricIds": ["documentation_presence"],
  "excludePatterns": [
    "(?:^|[/\\\\])node_modules(?:$|[/\\\\])",
    "(?:^|[/\\\\])dist(?:$|[/\\\\])"
  ]
}
```

## 設定の優先順位

後勝ちです。優先順位は次です。

1. デフォルト値
2. `analyzer.config.json`
3. `tsconfig.json`
4. CLI 引数
5. `.env`
6. 環境変数

重要なのは、環境変数が CLI 引数より強いことです。  
CI で `ANALYZER_OUTPUT_DIR` や `ANALYZER_PREFIX` を渡している場合、CLI 指定よりそちらが勝ちます。

## 環境変数

- `ANALYZER_OUTPUT_DIR`
- `ANALYZER_FORMATS`
- `ANALYZER_PREFIX`
- `ANALYZER_VERBOSE`
- `ANALYZER_CACHE_DIR`
- `ANALYZER_MAX_FILE_SIZE`
- `ANALYZER_COMPLEXITY_THRESHOLD`
- `ANALYZER_IMPACT_SCORE_THRESHOLD`
- `ANALYZER_FAIL_ON_IMPACT_THRESHOLD`
- `ANALYZER_LOG_FILE`
- `ANALYZER_MANUAL_INPUT`
- `ANALYZER_QUALITY_GATE_BLOCKING_METRICS`
- `ANALYZER_QUALITY_GATE_MONITORING_METRICS`

## 既定の除外対象

既定で次は除外されます。

- `node_modules`
- `dist`
- `build`
- `.next`
- `coverage`
- `.git`

さらに除外したい場合は `--exclude-patterns` または `analyzer.config.json` を使ってください。

## キャッシュ

キャッシュは 2 層です。

- file cache: `mtime + SHA256`
- analysis cache: 依存解析結果と複雑度解析結果の永続化

同じソース、同じ設定なら 2 回目以降は再計算が減ります。  
CI では `.ts-analyzer-cache` をキャッシュ対象に入れるべきです。

## 終了コード

- `0`: 成功
- `1`: 実行失敗
- `2`: `diff` で impact 閾値超過、または `quality gate` の出荷判定失敗

補足:

- `quality gate` も終了コード `2` を使います
- 条件は「親指標の自動 FAIL」または「baseline 比較での親指標回帰」です

CI の判定条件は `2` を明示的に拾う設計にすべきです。

## よくある失敗

### `diff` が baseline を読めず失敗する

原因はほぼ 2 つです。

- `--baseline` のパスが間違っている
- `--output` と `--prefix` を変えたのに baseline 側の命名規則が一致していない

最も安全な実行例:

```bash
node dist/src/cli.js analyze ./target-app --output ./analysis --prefix baseline
node dist/src/cli.js diff ./target-app --output ./analysis --prefix current --baseline ./target-app/analysis/baseline_report.json
```

### `graph` に `--format html` を付けても HTML が出ない

仕様です。  
`graph` は JSON と DOT 専用です。HTML が欲しければ `analyze` を使ってください。

### ログやレポートの出力先が想定と違う

原因は相対パスの基準の誤認です。  
`--output ./reports` は「このツールのディレクトリ」ではなく「解析対象の `projectDir`」基準です。

### Unitテスト通過率が MANUAL のまま変わらない

原因は 2 パターンです。

- JUnit XML が出ていない
- Vitest は検出されているが、実行結果 artifact を収集していない

このツールは `package.json` や `vitest.config.*` から Vitest を検出します。  
その場合の表示は `Vitest検出 / 結果未収集` です。通過率を自動計算したいなら JUnit XML を残してください。

## 実務での推奨パターン

### ローカル調査

```bash
node dist/src/cli.js analyze ../frontend --output ./analysis --prefix local --verbose
```

### 依存構造の棚卸し

```bash
node dist/src/cli.js graph ../frontend --output ./analysis --prefix architecture
```

### PR ゲート

```bash
node dist/src/cli.js diff ../frontend \
  --output ./analysis \
  --prefix pr \
  --baseline ../frontend/analysis/baseline_report.json \
  --impact-threshold 50 \
  --fail-on-impact
```

この運用にすると、PR で本当に危険な変更だけを機械的に弾けます。

### 出荷ゲート

```bash
node dist/src/cli.js quality gate ../frontend \
  --output ./analysis \
  --prefix release \
  --baseline ../frontend/analysis/release_quality_report.json \
  --quality-gate-monitoring-metrics documentation_presence
```

この運用にすると、出荷審査で落とす指標と監視専用指標を分けられます。

## 補足

ヘルプは次で確認できます。

```bash
node dist/src/cli.js --help
```
